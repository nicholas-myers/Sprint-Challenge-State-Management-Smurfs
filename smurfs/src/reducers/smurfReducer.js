const initialState = {
    loadingSmurfs: false,
    smurfs: [],
    smurfingError: ""
}

export const smurfReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}